# 🏗️ Repository Overview

> **What This Project Actually Does**

---

## 📋 Project Summary

| Aspect        | Details                                                |
| ------------- | ------------------------------------------------------ |
| **Name**      | Flask with PostgreSQL                                  |
| **Type**      | RESTful API Backend + Web Interface                    |
| **Purpose**   | Learning project for Flask + PostgreSQL                |
| **Language**  | Python 3.9+                                            |
| **Framework** | Flask 3.0                                              |
| **Database**  | PostgreSQL 15                                          |
| **API Style** | REST with OpenAPI documentation + URL-based versioning |

---

## 🎯 What This Application Does

This is a **user management API** that demonstrates production-grade Flask development. It provides:

### Core Features

```
┌─────────────────────────────────────────────────────────────────┐
│                      USER MANAGEMENT API                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  📝 User Registration                                            │
│     └── Create new user accounts with validation                 │
│                                                                  │
│  🔐 Authentication                                               │
│     └── Login with JWT tokens (access + refresh)                 │
│                                                                  │
│  👤 User Profile                                                 │
│     └── Read, update, delete user data                           │
│                                                                  │
│  📋 User Listing                                                 │
│     └── Paginated list of all users                              │
│                                                                  │
│  ❤️ Health Check                                                 │
│     └── API and database health status                           │
│                                                                  │
│  🌐 Web Interface                                                │
│     └── Landing page and health dashboard                        │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### API Endpoints (v1)

The API uses **URL-based versioning** with `/api/v1/` prefix:

| Method | Endpoint                 | Description                 |
| ------ | ------------------------ | --------------------------- |
| GET    | `/`                      | Landing page (HTML)         |
| GET    | `/health-dashboard`      | Health dashboard (HTML)     |
| GET    | `/api/v1/`               | API welcome message         |
| GET    | `/api/v1/health`         | Health check with DB status |
| POST   | `/api/v1/auth/login`     | Get JWT tokens              |
| POST   | `/api/v1/auth/logout`    | Revoke token                |
| POST   | `/api/v1/auth/refresh`   | Refresh access token        |
| GET    | `/api/v1/auth/me`        | Current user info           |
| POST   | `/api/v1/users/register` | Create new user             |
| GET    | `/api/v1/users/`         | List all users (paginated)  |
| GET    | `/api/v1/users/<id>`     | Get user by ID              |
| PUT    | `/api/v1/users/<id>`     | Update user                 |
| DELETE | `/api/v1/users/<id>`     | Soft delete user            |

### Documentation URLs

| URL                     | Description                   |
| ----------------------- | ----------------------------- |
| `/openapi/swagger`      | Swagger UI (interactive docs) |
| `/openapi/redoc`        | ReDoc (clean documentation)   |
| `/openapi/rapidoc`      | RapiDoc (alternative UI)      |
| `/openapi/openapi.json` | Raw OpenAPI specification     |

---

## 🆚 Express.js Comparison

If you've built an Express.js API, here's how this Flask app maps to what you know:

| Express.js             | This Flask App                        | Location          |
| ---------------------- | ------------------------------------- | ----------------- |
| `app.js` / `server.js` | `run.py`                              | Root              |
| `createApp()` factory  | `create_app()`                        | `app/__init__.py` |
| Express Router         | Flask Blueprint                       | `app/routes/`     |
| Nested routers         | Parent blueprints (versioning)        | `app/routes/v1/`  |
| Joi/Zod schemas        | Pydantic schemas                      | `app/schemas/`    |
| Mongoose models        | SQLAlchemy models                     | `app/models/`     |
| Middleware             | Decorators/Hooks                      | Throughout        |
| `package.json`         | `requirements.txt` + `pyproject.toml` | Root              |
| `.env`                 | `.env`                                | Same!             |
| `node_modules/`        | `venv/`                               | Root              |
| Jest/Mocha             | pytest                                | `tests/`          |
| Passport.js            | Flask-JWT-Extended                    | `app/auth/`       |
| Express Validator      | Pydantic                              | `app/schemas/`    |
| EJS/Pug views          | Jinja2 templates                      | `app/templates/`  |

---

## 🏛️ Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                    REQUEST → RESPONSE FLOW                       │
└─────────────────────────────────────────────────────────────────┘

     HTTP Request
          │
          ▼
    ┌─────────────┐
    │   Flask     │  ← App receives request
    │   Core      │
    └──────┬──────┘
           │
           ▼
    ┌─────────────┐
    │  Versioned  │  ← Parent blueprint handles /api/v1/*
    │  Blueprint  │     Routes to child blueprints
    └──────┬──────┘
           │
           ▼
    ┌─────────────┐
    │   Route     │  ← Child blueprint handles specific path
    │ (Blueprint) │     Validates input with Pydantic
    └──────┬──────┘
           │
           ▼
    ┌─────────────┐
    │   Service   │  ← Business logic layer
    │   Layer     │     All complex operations here
    └──────┬──────┘
           │
           ▼
    ┌─────────────┐
    │   Model     │  ← SQLAlchemy ORM
    │   (ORM)     │     Database operations
    └──────┬──────┘
           │
           ▼
    ┌─────────────┐
    │  PostgreSQL │  ← Actual database
    │  Database   │
    └─────────────┘
           │
           ▼
     HTTP Response
```

---

## 🔧 Technologies Used

### Core Stack

| Technology     | Purpose       | Node.js Equivalent |
| -------------- | ------------- | ------------------ |
| **Flask**      | Web framework | Express.js         |
| **SQLAlchemy** | ORM           | Mongoose/Prisma    |
| **PostgreSQL** | Database      | MongoDB/PostgreSQL |
| **Pydantic**   | Validation    | Joi/Zod            |
| **Jinja2**     | Templates     | EJS/Pug            |

### Extensions

| Extension          | Purpose                | Node.js Equivalent |
| ------------------ | ---------------------- | ------------------ |
| Flask-SQLAlchemy   | SQLAlchemy integration | -                  |
| Flask-Migrate      | Database migrations    | Prisma migrations  |
| Flask-JWT-Extended | JWT authentication     | Passport.js        |
| Flask-Limiter      | Rate limiting          | express-rate-limit |
| Flask-CORS         | CORS handling          | cors middleware    |
| flask-openapi3     | API documentation      | swagger-jsdoc      |

### Development Tools

| Tool       | Purpose         | Node.js Equivalent   |
| ---------- | --------------- | -------------------- |
| pytest     | Testing         | Jest/Mocha           |
| Black      | Code formatting | Prettier             |
| isort      | Import sorting  | eslint-plugin-import |
| flake8     | Linting         | ESLint               |
| mypy       | Type checking   | TypeScript           |
| pre-commit | Git hooks       | husky + lint-staged  |

### DevOps

| Tool           | Purpose           |
| -------------- | ----------------- |
| Docker         | Containerization  |
| docker-compose | Local development |
| GitHub Actions | CI/CD pipeline    |

---

## 📊 Project Statistics

```
┌────────────────────────────────────────┐
│         PROJECT AT A GLANCE            │
├────────────────────────────────────────┤
│                                        │
│  📁 Files:           ~100              │
│  📝 Python Files:    ~45               │
│  📄 Documentation:   ~35               │
│  🧪 Test Files:      ~10               │
│  🌐 HTML Templates:  ~3                │
│                                        │
│  📦 Dependencies:    ~15               │
│  🔧 Config Files:    ~10               │
│                                        │
│  ✅ Test Coverage:   ~80%              │
│                                        │
└────────────────────────────────────────┘
```

---

## 🎓 Learning Value

This project demonstrates:

1. **Python Best Practices**

    - Type hints
    - Docstrings
    - PEP 8 style

2. **Flask Patterns**

    - Application factory
    - Blueprints (with API versioning)
    - Extensions

3. **Database Design**

    - SQLAlchemy ORM
    - Migrations
    - Soft deletes

4. **API Design**

    - RESTful conventions
    - URL-based versioning
    - Standardized responses
    - Error handling

5. **Security**

    - Password hashing
    - JWT authentication
    - Rate limiting

6. **Code Organization**

    - Service layer
    - Clean architecture
    - Separation of concerns

7. **DevOps**
    - Docker
    - CI/CD
    - Automated testing

---

## 🔜 Next Steps

Now that you understand what this project does, let's look at how it's organized.

**Next: [0.3_folder_structure.md](./0.3_folder_structure.md)** - Complete breakdown of every folder and file.
